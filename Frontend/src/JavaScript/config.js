import { base_url } from "../../../render";

export const API_BASE_URL = `${base_url}/api/menu`;
export const API_HOME_MENU_URL = `${base_url}/api/homeMenu`;
export const API_TRANSACTION_URL = `${base_url}/api/homeMenu/transaction`;
export const API_TEAM_URL = `${base_url}/api/employee`;
export const API_ORDER_URL = `${base_url}/api/table`;
